`
  This script will run a sub-script and set any catches to a global variable.
`

("x<-" "net" ("$->" ("x->" "@") #1#))
("x<-" "bait" ("$->" ("x->" "@") #2#))
("x<-" "icebox" ("$->" ("x->" "@") #3#))

("^" "NET" ("x->" ("x->" "net")))

("x<-" "catch" ("NET" ("x->" "bait")))

("?" ("=" ("x->" "catch") "") ? ("x<-" ("x->" "icebox") ("x->" "catch")))
