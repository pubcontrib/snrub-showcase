`
  This script used to test and set global variables when local objects matches.
`

("x<-" "number" ("$->" ("x->" "@") #1#))
("x<-" "word" ("$->" ("x->" "@") #2#))

("?"
  ("=" ("x->" "source") ("x->" "number"))
  ("x<-" "destination" ("x->" "word"))
  ?
)
